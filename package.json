{
  "name": "ulohy",
  "version": "0.2.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "test": "jest",
    "test:watch": "npm test -- --watch"
  },
  "devDependencies": {
    "babel-jest": "^20.0.3",
    "babel-preset-env": "^1.6.0",
    "jest": "^20.0.4",
    "regenerator-runtime": "^0.11.0"
  },
  "jest": {
    "transform": {
      "^.+\\.js?$": "babel-jest"
    }
  }
}
